 🎓 Student Registration System
A simple Student Registration System built using HTML, CSS, and JavaScript.
This project allows users to add, view, edit, and delete student records with data stored in the browser using localStorage.

📌 Features
* Add new student details
* Edit existing student records
* Delete student records
* Data saved in browser (localStorage)
* Data persists even after page refresh
* Form validation (name, ID, email, contact)
* Modern glassmorphism UI

🛠 Technologies Used
* HTML5 – Structure of the application
* CSS3 – Styling and layout (Glass UI)
* JavaScript – Logic, validation, DOM manipulation
* localStorage – Permanent data storage in browser

📂 Project Structure
Student-Registration-System/
│
├── index.html      # Main HTML file
├── style.css       # Styling and UI design
├── script.js       # JavaScript logic
└── README.md       # Project documentation

🚀 How It Works
* User fills the student registration form
* JavaScript validates the input data
* Student data is saved in an array
* Array is stored in localStorage
* Table updates dynamically without page reload
* Data remains saved even after refreshing the page
